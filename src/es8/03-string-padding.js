const string = 'Hello';

console.log(string.padStart(6, '_'));
console.log(string.padEnd(6, '_'));

// string padding nos permite rellenar los espacios faltantes, en el primer parametro va la cantidad de caracteres y en el segundo con que se va a rellenar
