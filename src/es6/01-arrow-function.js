function square(num) {
    return num * num;
} //funcion normal

const square = (num) => {
    return num * num;
} // funcion flecha o arrow function

const square = num => num * num; // funcion con la palabra reservada return implicita solo se utiliza si hay un solo argumento.